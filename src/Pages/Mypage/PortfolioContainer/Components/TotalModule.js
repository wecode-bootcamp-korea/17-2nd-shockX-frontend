import React from "react";
import styled from "styled-components";

function TotalModule(props) {
  return <Head></Head>;
}

export default TotalModule;

const Head = styled.div`
  font-size: 16px;
  font-weight: 600;
`;
